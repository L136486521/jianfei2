name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        cache: 'pip'
        cache-dependency-path: 'requirements.txt'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install buildozer cython==0.29.33

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential git python3-dev python3-setuptools python3-pip python3-venv
        sudo apt-get install -y default-jdk zlib1g-dev libncurses5-dev libgdbm-dev libnss3-dev libssl-dev libreadline-dev libffi-dev curl libbz2-dev
        sudo apt-get install -y autoconf automake libtool pkg-config
        sudo apt-get install -y --no-install-recommends openjdk-11-jdk

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3
      with:
        sdk-version: '33'
        build-tools-version: '34.0.0'
        ndk-version: '21.4.7075529'
        cmdline-tools-version: 'latest'
    
    - name: Verify Android SDK installation
      run: |
        # 显示SDK配置信息
        echo "ANDROID_HOME: $ANDROID_HOME"
        echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
        echo "PATH: $PATH"
        
        # 显示SDK目录结构
        echo "SDK目录结构:"
        ls -la "$ANDROID_HOME"
        
        # 检查build-tools目录
        echo "build-tools目录:"
        ls -la "$ANDROID_HOME/build-tools/" || echo "build-tools目录不存在"
        ls -la "$ANDROID_HOME/build-tools/34.0.0/" || echo "build-tools/34.0.0目录不存在"
        
        # 检查关键工具
        echo "检查关键构建工具..."
        for tool in sdkmanager aidl zipalign aapt; do
          if command -v $tool &> /dev/null; then
            echo "✅ $tool工具已找到: $(command -v $tool)"
          else
            echo "❌ $tool工具未找到"
            # 查找工具可能的位置
            echo "正在查找$tool工具..."
            find "$ANDROID_HOME" -name "$tool" -type f
          fi
        done
        
        # 确保java版本正确
        echo "Java版本信息:"
        java -version
        javac -version

    - name: Configure Buildozer
      run: |
        # 初始化buildozer（如果需要）
        buildozer init || true
        
        # 确保使用正确的环境变量
        echo "配置Buildozer使用系统SDK..."
        echo "ANDROID_HOME: $ANDROID_HOME"
        echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
        
        # 检查并修复aidl工具
        echo "检查并修复aidl工具..."
        if ! command -v aidl &> /dev/null; then
          echo "❌ aidl工具未找到，尝试修复..."
          # 查找aidl工具可能的位置
          AIDL_PATH=$(find "$ANDROID_HOME" -name "aidl" -type f 2>/dev/null | head -1)
          if [ -f "$AIDL_PATH" ]; then
            echo "找到aidl工具: $AIDL_PATH"
            # 将aidl工具所在目录添加到PATH
            export PATH="$(dirname "$AIDL_PATH"):$PATH"
            echo "已将aidl工具所在目录添加到PATH"
          else
            echo "⚠️  未找到aidl工具，尝试创建软链接..."
            # 检查build-tools目录下是否有aidl工具
            AIDL_CANDIDATES=("$ANDROID_HOME/build-tools/34.0.0/aidl" "$ANDROID_HOME/build-tools/34.0.0/aidl3")
            for candidate in "${AIDL_CANDIDATES[@]}"; do
              if [ -f "$candidate" ]; then
                echo "找到aidl候选工具: $candidate"
                # 创建软链接
                ln -sf "$candidate" "$ANDROID_HOME/build-tools/34.0.0/aidl"
                export PATH="$ANDROID_HOME/build-tools/34.0.0:$PATH"
                echo "已创建aidl软链接并添加到PATH"
                break
              fi
            done
          fi
        else
          echo "✅ aidl工具已找到: $(command -v aidl)"
        fi
        
        # 更新buildozer.spec文件
        echo "更新buildozer.spec文件..."
        sed -i '/android.accept_sdk_license =/d' buildozer.spec
        sed -i '/android.sdk_path =/d' buildozer.spec
        sed -i '/android.build_tools_version =/d' buildozer.spec
        sed -i '/android.api =/d' buildozer.spec
        sed -i '/android.ndk_path =/d' buildozer.spec
        sed -i '/android.ndk_api =/d' buildozer.spec
        
        # 添加配置
        echo "android.accept_sdk_license = True" >> buildozer.spec
        echo "android.sdk_path = $ANDROID_HOME" >> buildozer.spec
        echo "android.build_tools_version = 34.0.0" >> buildozer.spec
        echo "android.api = 33" >> buildozer.spec
        echo "android.ndk_api = 21" >> buildozer.spec
        
        # 为buildozer创建环境配置
        mkdir -p ~/.buildozer/android/platform
        
        # 显示配置信息
        echo "Buildozer配置完成:"
        echo "ANDROID_HOME: $ANDROID_HOME"
        echo "PATH: $PATH"
        cat buildozer.spec | grep android.

    - name: Build APK
      run: |
        # 使用已设置好的环境变量
        echo "使用的环境变量:"
        echo "ANDROID_HOME: $ANDROID_HOME"
        echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
        echo "PATH: $PATH"
        
        # 检查buildozer目录结构
        echo "检查buildozer目录结构..."
        mkdir -p ~/.buildozer
        
        # 检查sdkmanager工具
        echo "检查sdkmanager工具..."
        if ! command -v sdkmanager &> /dev/null; then
          echo "❌ sdkmanager工具未找到，尝试修复..."
          # 查找sdkmanager工具可能的位置
          SDKMANAGER_PATH=$(find "$ANDROID_HOME" -name "sdkmanager" -type f 2>/dev/null | head -1)
          if [ -f "$SDKMANAGER_PATH" ]; then
            echo "找到sdkmanager工具: $SDKMANAGER_PATH"
            # 将sdkmanager工具所在目录添加到PATH
            export PATH="$(dirname "$SDKMANAGER_PATH"):$PATH"
            echo "已将sdkmanager工具所在目录添加到PATH"
          else
            echo "❌ 未找到sdkmanager工具"
            # 显示ANDROID_HOME目录结构，查找cmdline-tools
            echo "ANDROID_HOME目录结构:"
            ls -la "$ANDROID_HOME"
            ls -la "$ANDROID_HOME/cmdline-tools/" 2>/dev/null || echo "cmdline-tools目录不存在"
          fi
        else
          echo "✅ sdkmanager工具已找到: $(command -v sdkmanager)"
        fi
        
        # 再次检查关键工具
        echo "构建前再次检查关键工具..."
        for tool in sdkmanager aidl zipalign aapt; do
          if command -v $tool &> /dev/null; then
            echo "✅ $tool工具已找到: $(command -v $tool)"
          else
            echo "❌ $tool工具未找到"
            # 查找工具可能的位置
            echo "正在查找$tool工具..."
            find "$ANDROID_HOME" -name "$tool" -type f
          fi
        done
        
        # 明确设置buildozer使用的环境变量
        echo "设置buildozer特定的环境变量..."
        export ANDROID_SDK_HOME="$ANDROID_HOME"
        export ANDROID_SDK_ROOT="$ANDROID_HOME"
        export PATH="$PATH:$ANDROID_HOME/build-tools/34.0.0:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin"
        
        # 显示当前目录和文件
        echo "当前目录内容:"
        ls -la
        
        # 设置详细日志级别以便更好地调试
        export BUILDOZER_LOG_LEVEL=2
        export P4A_VERBOSE=1
        
        # 运行构建
        echo "开始构建APK..."
        buildozer --verbose android debug
      env:
        # 设置构建环境变量
        P4A_RELEASE_KEYALIAS: ${{ secrets.KEYALIAS }}
        P4A_RELEASE_KEYSTORE: ${{ secrets.KEYSTORE }}
        P4A_RELEASE_KEYSTORE_PASSWD: ${{ secrets.KEYSTORE_PASSWORD }}
        P4A_RELEASE_KEYALIAS_PASSWD: ${{ secrets.KEYALIAS_PASSWORD }}

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: weighttracker-apk
        path: bin/*.apk
