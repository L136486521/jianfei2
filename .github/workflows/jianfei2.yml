name: Build Android App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git unzip openjdk-11-jdk python3-pip \
          autoconf libtool pkg-config zlib1g-dev \
          libncurses5-dev libncursesw5-dev libtinfo5 \
          cmake libffi-dev libssl-dev

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install buildozer cython

    - name: Pre-download SDL2 dependencies (修复网络问题)
      run: |
        echo "🔄 预先下载SDL2_image的依赖..."
        mkdir -p sdl_deps
        cd sdl_deps
        
        # 下载libjpeg-turbo
        git clone --depth 1 --branch 2.0.1 https://github.com/libjpeg-turbo/libjpeg-turbo.git jpeg || true
        
        # 下载libpng  
        git clone --depth 1 --branch v1.6.37 https://github.com/glennrp/libpng.git libpng || true
        
        # 复制到buildozer的目录结构
        mkdir -p ../.buildozer/android/platform/build-arm64-v8a_armeabi-v7a/build/bootstrap_builds/sdl2/jni/SDL2_image/external
        cp -r jpeg ../.buildozer/android/platform/build-arm64-v8a_armeabi-v7a/build/bootstrap_builds/sdl2/jni/SDL2_image/external/ || true
        cp -r libpng ../.buildozer/android/platform/build-arm64-v8a_armeabi-v7a/build/bootstrap_builds/sdl2/jni/SDL2_image/external/ || true

    - name: Build APK with Buildozer
      run: |
        echo "🏗️ 开始构建APK..."
        buildozer -v android debug
      timeout-minutes: 35  # 给足时间

    - name: Upload APK artifact
      uses: actions/upload-artifact@v2
      with:
        name: android-app
        path: bin/*.apk
